version: '2'

services:
  website:
    build:
      context: ./
      dockerfile: Dockerfile
    environment:
      - VIRTUAL_HOST=www.blitzpath.in
      - LETSENCRYPT_HOST=www.blitzpath.in
      - LETSENCRYPT_EMAIL=contact@blitzpath.in
    network_mode: bridge
    restart: always
